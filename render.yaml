services:
  - type: web
    name: jothi-solar-solutions
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn solar_installation.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: "*"
      - key: DATABASE_URL
        fromDatabase:
          name: jothi-solar-db
          property: connectionString

databases:
  - name: jothi-solar-db
    plan: free
